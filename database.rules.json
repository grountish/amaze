{
  "rules": {
    "rooms": {
      ".read": "auth != null",
      "$roomId": {
        ".write": "auth != null",
        "players": {
          "$playerId": {
            ".write": "auth != null && (auth.uid === $playerId || $playerId.beginsWith('bot_'))",
            "hp": {
              ".write": "auth != null"
            }
          }
        },
        "events": {
          ".write": "auth != null"
        }
      }
    }
  }
}
